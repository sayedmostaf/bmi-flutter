import 'package:flutter/material.dart';

class BodyCompositionText extends StatelessWidget {
  const BodyCompositionText({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      "Body Composition",
      style: TextStyle(
        color: Colors.white.withOpacity(.5),
      ),
    );
  }
}
